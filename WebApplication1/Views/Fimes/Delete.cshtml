@model CatalogoFilmesTempo.Models.Filme
@using Microsoft.Extensions.Options
@{
    ViewData["Title"] = $"Excluir Filme: {Model.Titulo}";
}

<div class="container mt-4">
    <h2 class="text-danger">🗑️ Confirmação de Exclusão</h2>

    <div class="alert alert-danger" role="alert">
        <i class="fas fa-exclamation-triangle"></i>
        Você tem certeza que deseja remover o filme <strong>"@Model.Titulo"</strong> do seu catálogo local? Esta ação é irreversível.
    </div>

    <hr />

    <div class="row">
        <div class="col-md-6">
            <dl class="row">
                <dt class="col-sm-4">@Html.DisplayNameFor(model => model.Titulo)</dt>
                <dd class="col-sm-8"><strong>@Html.DisplayFor(model => model.Titulo)</strong></dd>

                <dt class="col-sm-4">@Html.DisplayNameFor(model => model.DataLancamento)</dt>
                <dd class="col-sm-8">@(Model.DataLancamento?.ToString("dd/MM/yyyy") ?? "N/D")</dd>

                <dt class="col-sm-4">@Html.DisplayNameFor(model => model.DuracaoMinutos)</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.DuracaoMinutos) min</dd>

                <dt class="col-sm-4">@Html.DisplayNameFor(model => model.CidadeReferencia)</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.CidadeReferencia)</dd>
            </dl>
        </div>
    </div>

    <hr />

    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <button type="submit" class="btn btn-danger me-2">
            <i class="fas fa-trash-alt"></i> Confirmar Exclusão
        </button>
        <a asp-action="Catalogo" class="btn btn-secondary">
            <i class="fas fa-arrow-left"></i> Cancelar e Voltar
        </a>
    </form>
</div>